generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["prismaSchemaFolder"]
  output          = "../../dist"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
